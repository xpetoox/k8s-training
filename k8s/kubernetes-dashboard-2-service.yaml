apiVersion: v1
kind: Service
metadata:
  name: kubernetes-dashboard-2
  namespace: kube-system
spec:
  type: ClusterIP
  selector:
    k8s-app: kubernetes-dashboard
  ports:
    - port: 8443
      targetPort: 8443